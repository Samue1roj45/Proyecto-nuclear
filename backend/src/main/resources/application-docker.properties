spring.datasource.url=jdbc:h2:file:/data/psicosocial;DB_CLOSE_DELAY=-1
spring.jpa.hibernate.ddl-auto=update

spring.h2.console.enabled=false

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost,http://localhost:4200}
app.jwt.secret=${JWT_SECRET:MisionPsicosocialSimuladorAcademicoSecretKey2024SuperSecure}
app.access-code.expiration-minutes=${ACCESS_CODE_EXPIRATION_MINUTES:10}
app.upload.dir=/app/uploads
